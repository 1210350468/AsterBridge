# Release validation

CI proves that the runtime builds, the launcher starts, and native packages pass their smoke
contract on macOS, Windows, and Linux. It does not prove an authenticated ChatGPT session, a live
MCP connector, or a complete Codex turn. A release candidate is not ready until those account-bound
flows are exercised manually on the platforms below.

## Required evidence

Record the release version, operating-system version, install path (`clean` or `upgrade`), ChatGPT
plan, Codex version, result of each check, and a redacted Activity log for every failure. Never
capture cookies, tunnel IDs, API keys, bearer tokens, or prompt contents.

The native package smoke is a separate prerequisite. On Windows it must use the completed NSIS process rather than a partial installation, verify `resources/runtime/runtime/bun.exe`, the runtime launcher and manifest before app startup, execute the durable installed runtime, and require the Launcher readiness marker to report tray availability. A valid Windows installation may exceed two minutes on slower storage; the smoke uses a bounded ten-minute installer budget so its own timeout cannot manufacture a half-installed package failure.

Electron 42 no longer downloads its native binary during package-manager `postinstall`. AsterBridge therefore treats Electron binary acquisition as an explicit package prerequisite rather than part of deterministic source verification. `bun run prepare:electron` invokes only the already-locked `launcher/node_modules/electron/install.js`, inherits the current proxy environment, verifies the platform-specific binary path, and uses bounded timeout/retry. CI and Release run this step after `verify` and before native packaging; every Launcher package entry point also performs the same idempotent prepare step so direct local packaging cannot silently depend on an old Electron cache.

The root core suite must run through `bun run test`, which intentionally executes every `tests/*.test.ts` file in its own deterministic short-lived Bun process. Do not replace this with one giant `bun test tests/*.test.ts` process in CI or Release workflows: Bun 1.4.0 can crash at the runtime level before test assertions finish. The runner must enumerate the complete test directory. It may retry only explicit Bun runtime-crash exit codes, at most twice by default; assertion/test failures such as exit code 1 must fail immediately and are never retried. On Windows + Bun 1.4.0 only, the eight short-lived named-pipe cases in `turn-broker-lifecycle.test.ts` are skipped because the Bun runtime can terminate before assertion reporting; Unix still runs those exact cases, while Windows broker behavior remains covered by server lifecycle, Full Harness, retained-compaction, DEV-driver, Launcher, and packaged-runtime tests.

## Windows 11 gate

Run this list on a maintained Windows 11 x64 machine with a real ChatGPT account. Treat it as a first-user journey, not just an internal feature checklist:

1. Install the packaged launcher on a clean Windows profile by following only `README.md` and `docs/quick-start.md`. Prove that no undocumented terminal step is required and that the embedded Bun runtime starts.
2. Prove network onboarding before model setup: with no explicit proxy, confirm **Automatic** reports the expected source; then test **Direct** and a valid **Custom** HTTP proxy. Verify local `127.0.0.1:17841` and Roxy `127.0.0.1:50000` stay bypassed through `NO_PROXY`, the managed daemon/tunnel restarts safely, and Doctor distinguishes `network-chatgpt` / `network-openai` failures from local Bridge health.
3. Prove the simplest embedded-browser path: sign in, reach a usable Temporary Chat composer, run **Doctor**, install the model route, restart Codex once, and complete the documented `WEB_OK` Browser-only turn.
4. Prove the recommended Roxy path from a clean Launcher state: save a Profile ID/data root, run Doctor, and complete one Browser-only turn through that exact Profile without copying cookies or browser state. Confirm AsterBridge proxy settings do not silently rewrite the Roxy profile's own proxy configuration.
5. Close the configured Roxy Profile completely, enable auto-open, and prove that Doctor reports either a reachable Profile or a healthy Local API ready to auto-open it. Start a turn and prove the Profile opens automatically instead of producing an unclassified stream disconnect.
6. During a live Roxy turn, prove Launcher **Live Preview** shows a real task frame/stage and **Take control** activates the matching Roxy task window without creating a second CDP owner. Let the same turn finish successfully afterward.
7. Prove every account-available ChatGPT Web effort appears exactly once in Codex without removing native models. A WebSocket `426` followed by successful HTTP/SSE fallback must not be reported to the user as a setup failure.
8. Prove Full-mode local tools through the primary MCP transport. Configure a fresh `Codex Native3` connector, run **Verify runtime**, complete one harmless MCP local-tool turn, and prove ChatGPT can remain in the same response across multiple dependent tool calls while outer Codex returns the real results. Start a fresh Full-mode Codex task whose outer registry exposes image generation and prove `image_gen__imagegen` reaches outer Codex, returns a real tool result, and writes the artifact under Codex `generated_images`. Retired `Codex Native` / `Codex Native2` identities must produce an explicit migration error rather than being silently reused. A ChatGPT first-party image result without an outer-Codex tool call/result is a release failure. The connectorless Responses bridge is an experimental fallback only: test it separately when desired, but its success does not replace the MCP stable-release gate and it must never be selected without explicit opt-in. Attach eight real images to a separate Web turn and prove all eight tiles complete, the adaptive attachment/send budgets are logged, submission is accepted without a duplicate retry, and the answer completes. Finally verify direct visual understanding, close every Temporary Chat page, and prove the resumed Codex thread can still answer a previously unstated visual detail from a historical image without attaching it again.
9. Prove conversation continuity independently of Temporary Chat lifetime: place an exact sentinel in a fresh Web thread, recover it on the next turn, close every Temporary Chat page and recover it again, then restart Launcher/daemon and recover it a third time. Verify retained continuation sends only the canonical suffix while a live page exists and falls back to bounded full-context replay after page loss/restart. Open enough completed Web threads to fill the five physical-page budget and prove the next new thread evicts the least-recently-used idle retained page; five genuinely active browser turns must still block a sixth. Record the Roxy ChatGPT-page count before and after Browser check / session inspection / connector verification and prove maintenance does not leave another page behind; at five retained surfaces it must evict one idle retained page instead of opening a sixth. Then drive a chat past the compaction threshold and prove that its handoff/retirement closes the superseded retained surface and continues without a duplicate/orphaned browser turn.
10. Cancel a running turn from Launcher and cancel another from Codex; prove neither recreates a browser page nor leaves HTTP/browser activity counters busy.
11. Quit/reopen the launcher around an idle runtime and around an active-turn cancellation. Prove stale-owner recovery is bounded, the saved browser/Roxy/proxy configuration survives, and the Responses daemon returns to `127.0.0.1:17841` without launching duplicate owners. On Windows, also reopen during a same-version packaged-runtime refresh while the managed MCP/Tunnel child still has the previous Bun executable open: the validated replacement must continue launching, `.previous-*` cleanup must be non-fatal, and a later launch must reclaim the stale directory after handles are released.
12. Disconnect the bridge and prove that the exact previous Codex route is restored. Reconnect it and prove existing private MCP/Roxy credentials are reused rather than exposed or replaced.
13. Upgrade from the previous public release. Prove Launcher state, Roxy/proxy settings, browser state, Codex settings, and private MCP configuration survive; prove old `Codex Native2` local configuration is migrated to the required `Codex Native3` identity with a clear user action.
14. Run **Settings → Run diagnostics** in at least four failure states (bad proxy/public upstream unreachable, closed Roxy without auto-open, invalid/unreachable Local API, stopped Tunnel). Confirm each failure points to the correct recovery action. Use **Copy diagnostic summary** and verify the clipboard text contains no API credentials, proxy credentials, token, cookie, full turn token, prompt body, or Doctor `detail` field.
15. Open the GitHub bug form from a clean browser and verify its Quick Start/Troubleshooting links resolve, the required fields match the support workflow, and the security checklist prevents accidental credential sharing.

Any failed or unexecuted item blocks a stable release. An alpha may ship with a named failed item
only when the release notes describe the limitation and recovery path explicitly.

### v3.0.31 model-catalog refresh result

Windows 11 x64 live diagnosis on 2026-09-05 reproduced a bridge-only native model staleness bug after GPT-6 Astra rollout. With the v3.0.30 route disconnected, Codex **0.153.1** refreshed `models_cache.json` directly from the official service and the cache contained `gpt-6-astra`, `gpt-5.6-sol`, and `gpt-5.6-terra`. The v3.0.30 reconnect path would previously authenticate and reuse the older AsterBridge managed catalog, then delete that fresher provider cache. The repaired reconnect now rebuilds the static managed catalog from that direct cache before reinstalling `model_catalog_json`, updates the journaled catalog SHA in the same compensated transaction, then invalidates the provider cache. The live repaired reconnect produced one managed catalog containing `gpt-6-astra` plus the account-eligible `chatgpt-web/light`, `chatgpt-web/medium`, and `chatgpt-web/high` routes. Focused Codex integration coverage passes **27 / 27** with TypeScript PASS.

The 3.0.31 exact-source local release gate then passed Core **42 / 42** deterministic batches, Launcher **212 pass / 0 fail / 1 Windows-inapplicable skip** (**213 total**), TypeScript, renderer production build, runtime bundle/license generation, `RELOCATABLE_RUNTIME_SMOKE_OK`, and `DEPENDENCY_AUDIT_OK root+launcher` with **0 vulnerabilities / 106 root packages** and **0 vulnerabilities / 351 launcher packages**. The final local Windows installer `asterbridge-3.0.31-win-x64.exe` is **161,916,884 bytes**, its blockmap is **169,421 bytes**, and installer SHA-256 is `F4EE55DA81B5CB2F34A1FD3289B657F25D7AE2C3EE53A2823174DD9EAB37B051`. Native package smoke completed with `PACKAGED_LAUNCHER_SMOKE_OK win32/x64`; the production Launcher then upgraded the existing Full/MCP 3.0.30 state to 3.0.31 and installed durable runtime bundle id `6ced941fb20203bb6c9652aa58a01bebdb2e875f39598b9ad7bf984aa54cf6fb`.

Installed-runtime validation repeated the failing user journey rather than relying on the source fixture. With bridge active, 3.0.31 health reported `status=ok`, `mode=full`, `accepting_turns=true`, and no active HTTP/browser turns; Doctor returned `ok=true`, RoxyBrowser was reachable, Launcher owned the daemon and Tunnel, the Tunnel reported healthy/ready, and a separate browser verification found `Codex Native3`. The installed 3.0.31 CLI disconnected the bridge, Codex 0.153.1 refreshed `models_cache.json` at 2026-09-05 19:00 local time with `gpt-6-astra` at the head of the official catalog, then the installed 3.0.31 CLI reconnected. The resulting AsterBridge managed catalog still contained `gpt-6-astra`, `gpt-5.6-sol`, and all three account-eligible Web routes, while the provider cache was removed after the compensated catalog/journal commit. This is the exact bridge-on state that 3.0.30 previously failed.

### v3.0.30 local result

Maintainer validation on Windows 11 x64 on 2026-09-04 used the exact packaged/installed 3.0.30 artifact with the authenticated RoxyBrowser profile, `Codex Native3`, and the primary MCP Full Harness transport. Final installed Doctor reported configuration valid, Roxy profile reachable, Codex route installed, Launcher ownership healthy, Responses proxy healthy on `127.0.0.1:17841`, ChatGPT/image upstream probes reachable, Tunnel runtime key stored privately, Launcher-owned Tunnel healthy/ready, and explicit browser verification confirmed `ChatGPT connector "Codex Native3" is available in the configured external browser.` Production health returned to `active_http_turns=0` and `active_browser_turns=0` after the release soak.

Final exact-source verification passed after the RC dependency/runtime fixes: Core **42 / 42**, Launcher **212 pass / 0 fail / 1 Windows-inapplicable skip** (**213 tests total**), TypeScript PASS, renderer production build PASS, and `RELOCATABLE_RUNTIME_SMOKE_OK`. The bounded security gate is also green: root **0 vulnerabilities / 106 packages**, launcher **0 vulnerabilities / 351 packages**, `DEPENDENCY_AUDIT_OK root+launcher`. RC hardening keeps that network-dependent security check separate from deterministic source verification; either audit must succeed before GitHub Release publication, while an advisory-service outage no longer turns `verify` red. The first public `v3.0.30-rc.1` CI run additionally exposed that repeat setup could depend on a system Codex CLI after the first managed install intentionally removed `models_cache.json`; RC hardening now permits reuse of the previous managed catalog only when a v8/v9 journal authenticates its exact path and SHA-256, while legacy unauthenticated journals still require a native/cache source and tampering remains fail-closed. A subsequent shared Unix-runner failure isolated to the tokenizer-heavy Luna rolling-checkpoint correctness case was treated as CI timing noise rather than a production regression: fresh Linux containers completed the case in roughly 0.1–0.2 s, while one GitHub runner exceeded Bun's global 5 s default. That single test now has a local 15 s timeout, the global timeout is unchanged, and a clean Linux Core run passed all **42 / 42** deterministic batches. Electron **42.9.0** and electron-builder **26.16.0** replace the vulnerable legacy launcher dependency path; Electron's explicit binary bootstrap now uses the same automatic proxy resolver as AsterBridge and successfully selected the maintainer Windows system proxy at `127.0.0.1:10809`. The first GitHub macOS clean runner exposed one bootstrap path-join regression where `Electron.app` was resolved before joining Electron's locked `dist` directory; the macOS executable path is now kept relative until the final join, and the packaging contract prevents that absolute-path regression. The maintainer-local Windows package `asterbridge-3.0.30-win-x64.exe` is **161,916,079 bytes** with a **169,367-byte** blockmap, SHA-256 `81C85653F083B4BFF4481FE013E4F9E1ECBDDA643D18650A5A181EF263157740`, and passed `PACKAGED_LAUNCHER_SMOKE_OK win32/x64`. GitHub Release **`v3.0.30-rc.2`** was then published as a prerelease from commit `24fe63e`; Release run **33882607991** passed audit plus Linux amd64, Windows amd64, macOS arm64, macOS Intel/amd64 build+package+smoke and the final checksum/publish job. The release contains **18 assets**. The authoritative published Windows installer is **161,916,033 bytes**, SHA-256 `4C106207453AACE4EBC2FFFCEBA7F5FC319D813B7B6EE2C4AEDBDB301721FD10`; because this differs slightly from the maintainer-local bytes, AsterBridge does not claim bit-reproducible installer output across builders and the published checksum is the release authority. During the same validation, reopening the installed Launcher with a live MCP/Tunnel worker exposed a Windows file-lock edge case: a successfully replaced same-version runtime could leave `.previous-*` locked and cleanup raised fatal `EPERM`; cleanup is now best-effort after replacement validation and stale previous directories are reclaimed on a later launch.

Stable promotion of `v3.0.30-rc.2` was stopped after Codex Desktop **26.901.2854.0** surfaced a newer native compaction protocol boundary. With AsterBridge's intentionally managed `remote_compaction_v2 = false`, the bundled Codex **0.153.0** core sent legacy `POST /responses/compact`, while the authenticated ChatGPT Codex backend had retired that native endpoint and returned **404 Not Found**. The repair keeps bounded v1 replacement history for ChatGPT Web routes, but on a native-only upstream 404 translates the operation to the current streaming Responses `compaction_trigger` contract, consumes SSE incrementally through `response.completed` without trusting a normalized `Content-Type`, preserves the single official opaque compaction item, and returns it inside the bounded replacement history expected by the legacy caller. Non-404 failures and ambiguous/missing compaction output remain fail-closed. Real same-owner E2E now passes on both Codex **0.150.1** and the exact Desktop-bundled **0.153.0** core with `remote_compaction_v2=false`: each run observed the retired endpoint 404, activated the compatibility path, logged `modern fallback PASS`, emitted `contextCompaction`, resumed the same thread, and returned `ASTERBRIDGE_CODEX_COMPACTION_OK`.

The repaired commit `a642818` then passed the full RC3 promotion chain. Main CI passed audit, actionlint, Ubuntu, Windows, and macOS verify+package+smoke; Release run **33899700221** published **`v3.0.30-rc.3`** after Linux amd64, Windows amd64, macOS arm64, macOS Intel/amd64 and final publish/checksum all succeeded. The authoritative RC3 Windows installer was **161,916,694 bytes**, SHA-256 `8a8c0bbe8beb09c70560ef989c1ebe9e94bdd6743316995a526b3653daa0a63a`. That exact GitHub asset was downloaded again, checksum-verified, installed, passed packaged readiness (`packaged=true`, `runtimeVerified=true`, `trayReady=true`), and its installed runtime repeated the Codex Desktop **0.153.0** legacy-compaction E2E successfully. After that asset-level gate, stable tag **`v3.0.30`** was created on the same verified commit. Stable Release run **33903185346** passed bounded audit plus Linux amd64, Windows amd64, macOS arm64, macOS Intel/amd64 build+package+smoke and final publish/checksum. The resulting GitHub Release is `draft=false`, `prerelease=false`, contains **18 assets**, and the authoritative stable Windows installer is **161,916,694 bytes**, SHA-256 `a67f0629f4d780f16b2db92e3d8b47646a4fa19f880265ed12ed172d7e3b5e5b`.

The repaired RC3 candidate is locally green on Windows 11 x64: deterministic Core **42 / 42**, Launcher **212 pass / 0 fail / 1 skip**, renderer/runtime/license gates PASS, `RELOCATABLE_RUNTIME_SMOKE_OK`, and `DEPENDENCY_AUDIT_OK root+launcher` with **0 vulnerabilities / 106 root packages** and **0 vulnerabilities / 351 launcher packages**. The local installer is **161,916,739 bytes**, blockmap **169,383 bytes**, SHA-256 `F6422F3E14A2D7B828160DAB4D689B8ED86D17AE7D4B68171B55097753D50AE6`. Silent installation produced packaged runtime bundle id `f90694fa3fd2359c42faee929a40e56ac29772fd627489d2f2fd3156dfef2df`; a direct smoke of the installed `AsterBridge.exe --launcher-smoke-test` exited 0 and wrote `ok=true`, `packaged=true`, `runtimeVerified=true`, `trayReady=true`. GitHub RC3 remains authoritative for the baseline-Bun, cross-platform build/package/smoke and published checksums.

The installed long-chain regression that exposed 3.0.27 replay was rerun after the 3.0.28 deferred-subagent fix: the pre-subagent shell and image-view stages executed once, deferred spawn reached outer Codex, the child completed through wait/close, and the parent finished without rebuilding the browser session or replaying earlier successful tools. Explicit Launcher cancellation was then tested against a real already-running local shell. AsterBridge cancelled Web/HTTP ownership and tombstoned the native execution key; the already-started outer-Codex tool was allowed to finish once, while five subsequent Codex reconnect attempts were rejected as `turn_cancelled` before browser creation. Rollout evidence contained **1 function call / 1 command completion** for the cancelled command and final activity returned to 0/0.

Native retained compaction also passed against Codex 0.150.1. Two retained Web turns compacted into a real `contextCompaction` on the same owner and the full E2E script returned `ASTERBRIDGE_CODEX_COMPACTION_OK` with `sameOwner=true`. The resulting compacted thread was resumed and `Codex Native3` executed `Write-Output POST_COMPACT_MCP_330_OK` with exit code 0, proving MCP tool work continues after replacement-history compaction. 3.0.30 keeps structured one-shot MCP compaction handoff authoritative, but if the dedicated retained compaction response completes with a valid checkpoint and current ChatGPT skips that control call, AsterBridge allows only a short grace interval, revokes the one-shot control token, canonicalizes that exact retained-turn checkpoint, and rejects any late stale handoff.

A mixed Full-Harness soak completed shell → outer Codex image generation → native subagent spawn/wait/close → final answer in one parent task. The parent executed `Write-Output MIX330_SHELL_OK` once, emitted one `imagegen` call in namespace `image_gen` followed by completed extension output, wrote one **1254×1254** PNG (**825,948 bytes**, SHA-256 `19561F12D2CA53AFCFA7724DADC15B2C19D07BA6EB4F0BD50BA92791FE9E4E96`) under that parent thread's `generated_images` directory, completed the child agent, and returned `MIX330_OK`. A DevPilot control-channel 502 launched the test command twice as two independent root `codex_exec` processes with different Codex thread ids; each root completed independently with its own artifact. This is test-controller duplication, not same-turn AsterBridge replay, and must not be used as evidence for or against one-turn exactly-once semantics.

Concurrency soak reached five simultaneous browser turns and a sixth failed closed at the five-page safety ceiling. Under five-way MCP-tool pressure, ChatGPT Custom App/Tunnel produced intermittent upstream 502s before some calls reached AsterBridge's local TurnBroker; the local broker did not lose an accepted call, no orphaned test shell remained, all browser/HTTP ownership returned to 0/0, and Launcher crash/restart restored Full MCP, Tunnel readiness, Roxy reachability, and connector verification. Therefore the five-page limit is a browser/resource safety ceiling, not a guarantee that the upstream ChatGPT App/Tunnel can sustain five local-tool executions concurrently.

### v3.0.25 MCP-primary restoration

3.0.25 restores the release contract that existed before the connectorless experiment: MCP/`Codex Native3` is the primary Full Harness transport and the Launcher no longer advertises or automatically preserves Responses Full as the recommended tool path. The Responses direct-tool implementation remains available only through explicit advanced/CLI opt-in for experiments on accounts without custom MCP Apps. Stable promotion requires the MCP gate above; the 3.0.24 Responses results below remain historical fallback evidence, not a substitute for MCP validation.

### v3.0.24 local result

Maintainer validation on Windows 11 x64 on 2026-09-02 used the exact packaged/installed 3.0.24 artifact with the authenticated RoxyBrowser profile and the connector-independent Responses Full transport. Production config was migrated by the packaged CLI to `mode=full`, `localToolTransport=responses`, with no active `tunnel` block. Doctor reported `ok=true`, the configured Roxy profile reachable, the Codex route installed, the Launcher owning the background runtime, the Responses proxy healthy on `127.0.0.1:17841`, the ChatGPT/image upstream probes reachable, and `Responses tool bridge is enabled; outer Codex owns tool approval, sandboxing, and execution`.

The exact Windows artifact `asterbridge-3.0.24-win-x64.exe` is 151,524,624 bytes with a 158,095-byte blockmap. The installed manifest reports appVersion `3.0.24` and bundle id `229a06ca04442562a26b542d98e0bdbc828c7399d074f473d9bc77a88c88049b`; packaged `AsterBridge.exe`, Bun, and CLI are present. A Windows electron-builder deadlock in the default Electron-cache unpack step was reproduced and removed by pinning `build.electronDist` to the repository's locked unpacked Electron 41.7.1 distribution; the repaired build completed through NSIS normally.

Real Plus-account Full Harness E2E passed without a ChatGPT custom MCP App or Tunnel. A shell probe caused outer Codex to emit `command_execution`, PowerShell executed `Write-Output RESPONSES_BRIDGE_324_OK`, returned exit code 0, and the same Web turn returned the final sentinel. A separate image-generation probe emitted a direct Responses `function_call` named `imagegen` with call id `dtc_198b26ab82a7b592d16d8bb3`, followed by completed extension execution and a matching `function_call_output`; the Web turn then returned `IMAGEGEN_324_OK`. Codex `generated_images` increased from 85 to 86 files and the new artifact is a valid 1254×1254 PNG, 858,753 bytes, SHA-256 `5B6081312C25A326FDC489094F08E4667342C45E5386943C378497CB7D84C74A`. Production health returned to `active_http_turns=0` and `active_browser_turns=0` afterward.

The earlier 3.0.19 Browser-only compaction/native-switch/eight-image/cancellation/restart/route evidence below remains valid background coverage. The 3.0.24 Responses-Full E2E proves the connectorless fallback can execute shell and image tools, but 3.0.25 no longer treats that path as the stable Full Harness default. Stable publication again requires MCP/`Codex Native3` validation plus the final exact-source verification and repository publication decision.

### v3.0.19 local result

Maintainer validation on Windows 11 x64 on 2026-09-02 used the packaged/installed 3.0.19 runtime plus the authenticated RoxyBrowser profile. Package installation and the packaged Launcher smoke passed with a verified `3.0.19 / win32 / x64` manifest and readiness marker `ok=true`, `packaged=true`, `runtimeVerified=true`, `trayReady=true`. Production route migration reached v9 with `openai_base_url`, the managed `model_catalog_json`, and `[features].remote_compaction_v2 = false`; Doctor/health subsequently reported a healthy 3.0.19 Responses daemon with no active turns.

The critical automatic-compaction regression was reproduced through an isolated installed Browser-only 3.0.19 daemon while preserving the production Full-mode configuration. A real `chatgpt-web/light` thread reached `lastInput=34,760` against its 31,980-token window; the following tiny turn emitted a Codex `compacted` record plus `ContextCompaction`, then completed at `17,639 / 31,980` and returned `ISO319_AFTER_REAL_COMPACT_OK`. The same compacted thread switched to native `gpt-5.6-sol` and returned `NATIVE_AFTER_V1_COMPACT_319_OK` without an `ocx1` encrypted-content verification failure, then survived a complete daemon stop/start and returned `RESTART_CONTINUITY_319_OK`. A real eight-image Browser-only High turn completed with `EIGHT_IMAGES_HIGH_319_OK` at 51,487 input tokens; Instant correctly rejected the same eight-image payload because that request exceeded its 31,980-token model budget. Forced client cancellation returned `active_http_turns=0` and `active_browser_turns=0`. Production route disconnect removed the managed route/catalog/feature lines and reconnect restored them with `errors=[]` and a healthy `127.0.0.1:17841` daemon. Final source verification remained **41/41 core files PASS**, Launcher **203 pass / 0 fail / 1 Windows-inapplicable skip**, TypeScript/renderer build PASS, and `RELOCATABLE_RUNTIME_SMOKE_OK`.

Historical 3.0.19 blocker: the maintainer ChatGPT Plus session did not expose the configured `Codex Native3` custom MCP app, so the MCP-only Full Harness could not be re-proven at that version. 3.0.20-3.0.24 added a connectorless Responses fallback and proved shell/image execution on the same Plus account. In 3.0.25 that fallback remains available for explicit experiments, but the project returns to MCP as the stable Full Harness contract rather than redefining Full around repeated Web-response tool envelopes.

### v3.0.10 local result

Maintainer validation passed locally on Windows 11 x64 on 2026-08-31 using the packaged 3.0.10 installer and a real ChatGPT account through RoxyBrowser + `Codex Native3`. Route inspection reported active with `errors=[]`; Doctor was ready and the Responses proxy was healthy on `127.0.0.1:17841`; connector verification succeeded in the configured external browser. Fresh-thread conversation sentinels survived ordinary turns, forced closure of every Temporary Chat page, and a full Launcher/daemon restart. Direct vision and historical-image replay both passed. A no-tool text-only Full Harness E2E emitted only the requested assistant text with no command/tool item. A retained-surface stress run held the physical Temporary Chat count at five and logged idle LRU releases instead of failing the next completed-thread allocation. A final fresh image-generation regression still queued `image_gen__imagegen` and produced a real 1254×1254 RGB PNG (~1.1 MB), proving the reduced tool bias does not suppress required native capabilities. Core **41/41**, Launcher **202 pass / 0 fail / 1 Windows-inapplicable skip**, TypeScript/renderer build, and `RELOCATABLE_RUNTIME_SMOKE_OK` all passed. GitHub publication remains a separate gate.

### v3.0.0 result

Maintainer validation passed on Windows 11 x64 on 2026-08-22 using the published v3.0.0-alpha
upgrade package and a real ChatGPT Pro account. The authenticated launcher, Codex model catalog,
Full-mode MCP tools, Pro turns, compaction, cancellation, session reuse, and preserved connector
configuration were exercised successfully. The direct installer completed successfully but gave no
clear completion action; v3.0.0 changes it to an assisted installer with a final launch option.

## macOS gate

Repeat items 2 through 10 on the oldest supported macOS version or the closest maintained machine.
Packaging smoke and code-signing verification remain separate gates; neither substitutes for the
interactive account flow.

## Linux gate

CI packaging smoke is required. Before claiming interactive Linux support for a release, repeat
items 2 through 7 under a supported desktop session and record the display server and packaging
format used.
